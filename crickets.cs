using System.Collections; //Импортируются стандартные библиотеки
using System.Collections.Generic;
using UnityEngine; //для юнити

public class crickets : MonoBehaviour //Объявляем новый класс crickets - наследник MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("crickets"); //функция Debug.Log(), чтобы вывести строку crickets в консоль Unity
    }
}
